# Ultralytics YOLOMM 🚀 RT-DETR Asymmetric Frequency-Guided Fusion
# 核心策略: 跨尺度红外频域先验引导 (IR_P5->RGB_P4, IR_P4->RGB_P3) + RT-DETR Decoder

# Parameters
nc: 80 # number of classes
scales:
  l: [1.00, 1.00, 1024]

backbone:
  # ==================== RGB 路径 (层 0-7) ====================
  - [-1, 1, ConvNormLayer, [32, 3, 2, None, False, 'relu'], 'RGB'] # 0-P1/2
  - [-1, 1, ConvNormLayer, [32, 3, 1, None, False, 'relu']]        # 1
  - [-1, 1, ConvNormLayer, [64, 3, 1, None, False, 'relu']]        # 2
  - [-1, 1, nn.MaxPool2d, [3, 2, 1]]                               # 3-P2/4

  - [-1, 1, Blocks, [64, BasicBlock, 2, 2, 'relu']]                # 4
  - [-1, 1, Blocks, [128, BasicBlock, 2, 3, 'relu']]               # 5-P3/8 (RGB_P3) 提取
  - [-1, 1, Blocks, [256, BasicBlock, 2, 4, 'relu']]               # 6-P4/16 (RGB_P4) 提取
  - [-1, 1, Blocks, [512, BasicBlock, 2, 5, 'relu']]               # 7-P5/32 (RGB_P5) 提取

  # ==================== X 路径 (红外/深度) (层 8-15) ====================
  - [-1, 1, ConvNormLayer, [32, 3, 2, None, False, 'relu'], 'X']   # 8-P1/2
  - [-1, 1, ConvNormLayer, [32, 3, 1, None, False, 'relu']]        # 9
  - [-1, 1, ConvNormLayer, [64, 3, 1, None, False, 'relu']]        # 10
  - [-1, 1, nn.MaxPool2d, [3, 2, 1]]                               # 11-P2/4

  - [-1, 1, Blocks, [64, BasicBlock, 2, 2, 'relu']]                # 12
  - [-1, 1, Blocks, [128, BasicBlock, 2, 3, 'relu']]               # 13-P3/8 (X_P3) 抛弃
  - [-1, 1, Blocks, [256, BasicBlock, 2, 4, 'relu']]               # 14-P4/16 (X_P4) 提取
  - [-1, 1, Blocks, [512, BasicBlock, 2, 5, 'relu']]               # 15-P5/32 (X_P5) 提取

# ==================== 方案二：微小目标特化方案 ====================
  # P5: 引入 AFGCAttention (自适应特征引导)，在低分辨率下提供最强的全局上下文引导
  - [[7, 15], 1, SymmetricFreqGuidedFusion_attn, [512, False, 2, 'AFGCAttention']]
  - [-1, 1, C2PSA, [512]]   
  # P4: 引入 LSKBlock (大核选择机制)，动态调整感受野，分离红外模糊热源与可见光实体
  - [[6, 15], 1, DecoupledFreqGuidedFusion_attn, [256, False, 2, 'LSKBlock']]
  # P3: 引入 CoordAtt (坐标注意力)，将精细的 X/Y 位置信息嵌入通道，防止小目标坐标偏移
  - [[5, 14], 1, DecoupledFreqGuidedFusion_attn, [128, False, 2, 'CoordAtt']]
# RT-DETR 专属 Transformer 检测头
head:
  # ============ P5 尺度 (AIFI) ============
  - [17, 1, Conv, [256, 1, 1, None, 1, 1, False]]  # 20: 引入 Fused_P5(17) 投影到 256
  - [-1, 1, AIFI, [1024, 8]]                       # 21:
  - [-1, 1, Conv, [256, 1, 1]]                     # 22: Y5

  # ============ FPN (Top-down) ============
  - [-1, 1, nn.Upsample, [None, 2, 'nearest']]     # 23: Y5 上采样
  - [18, 1, Conv, [256, 1, 1, None, 1, 1, False]]  # 24: 引入跨尺度 Fused_P4(18) 并投影
  - [[-2, -1], 1, Concat, [1]]                     # 25:
  - [-1, 3, RepC3, [256, 0.5]]                     # 26: fpn_blocks.0
  - [-1, 1, Conv, [256, 1, 1]]                     # 27: Y4

  - [-1, 1, nn.Upsample, [None, 2, 'nearest']]     # 28: Y4 上采样
  - [19, 1, Conv, [256, 1, 1, None, 1, 1, False]]  # 29: 引入跨尺度 Fused_P3(19) 并投影
  - [[-2, -1], 1, Concat, [1]]                     # 30:
  - [-1, 3, RepC3, [256, 0.5]]                     # 31: X3 (P3尺度最终特征)

  # ============ PAN (Bottom-up) ============
  - [-1, 1, Conv, [256, 3, 2]]                     # 32: downsample P3
  - [[-1, 27], 1, Concat, [1]]                     # 33:
  - [-1, 3, RepC3, [256, 0.5]]                     # 34: F4

  - [-1, 1, Conv, [256, 3, 2]]                     # 35: downsample P4
  - [[-1, 22], 1, Concat, [1]]                     # 36:
  - [-1, 3, RepC3, [256, 0.5]]                     # 37: F5

  - [[31, 34, 37], 1, RTDETRDecoder, [nc, 256, 300, 4, 8, 3]] # 38: 端到端检测